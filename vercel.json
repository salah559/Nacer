{
  "buildCommand": "pnpm install && cd artifacts/nesda-presentation && BASE_PATH=/ PORT=3000 pnpm run build && cd ../.. && mkdir -p public && cp -r artifacts/nesda-presentation/dist/public/* public/",
  "outputDirectory": "public",
  "installCommand": "echo 'installed via buildCommand'",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
