{
  "buildCommand": "pnpm install && (cd artifacts/nesda-presentation && BASE_PATH=/ PORT=3000 pnpm exec vite build --config vite.vercel.config.ts)",
  "outputDirectory": "public",
  "installCommand": "echo 'installed via buildCommand'",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
