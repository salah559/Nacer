{
  "buildCommand": "sh -c 'BASE_PATH=/ PORT=3000 pnpm --filter @workspace/nesda-presentation run build && mkdir -p public && cp -r artifacts/nesda-presentation/dist/public/* public/'",
  "outputDirectory": "public",
  "installCommand": "echo 'installed via buildCommand'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
